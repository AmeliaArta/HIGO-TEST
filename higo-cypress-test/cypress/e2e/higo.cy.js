describe('Higo.id Landing Page', () => {
  //beforeEach(() => {
  //  cy.visit('https://higo.id')
  //})
  it('displays load homepage Successfully', () => {
    cy.visit('https://higo.id');
  //  cy.title().should('include', 'Higo'); //melakukan pengecekan Tambahan Judul
  });
});

